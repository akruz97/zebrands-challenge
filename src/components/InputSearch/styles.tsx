import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containerInput: {
    height: 50,
    borderWidth: 0.2,
    borderColor: "#4c4c4c",
    margin: 5,
    borderRadius: 10,
    padding: 10,
  },
});
